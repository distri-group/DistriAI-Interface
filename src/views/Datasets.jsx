import styled from "styled-components";

function Datasets({ className }) {
  return (
    <div className={className}>
      <span>Datasets</span>
    </div>
  );
}

export default styled(Datasets)`
  width: 1200px;
  margin: 0 auto;
`;
